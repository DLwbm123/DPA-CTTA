# Launch lock repair — 2026-09-25

Initial launch at code 57e3764 stalled in NAS NLM blocking locks. All three workers and queue were observed in nlmclnt_wait; a separate three-process NAS flock probe reproduced the timeout. The queue and owned workers were terminated, preserving outputs and reservations. Initial observations showed zero calls; final live evidence showed two forward calls in shard 0 and no committed training step. Do not describe the failed launch as zero compute or zero cost.

Repair at 2329fb6: use a user-owned local lock inode, require the recorded hostname on each ledger operation, and retain all ledger data/checkpoints on NAS. This changes synchronization only. Workers remain restricted to physical GPUs 5/6/7 on the same host. Thirty concurrent reserve/settle operations on the repaired real NAS ledger passed.

All three workers left initial tensor snapshots with zero committed anchors, empty results, empty physical logs and preserved CPU/CUDA RNG. Each was validated against its original identity, then migrated to the repaired code identity with the scientific protocol and graph unchanged. Shard 1 lacked its final atomic sidecar; that sidecar alone was reconstructed in a recovery copy from the complete tensor snapshot and the full journal validator was run before migration. Original files were retained. All three new workers used the existing verified recovery routine, and each has exhausted its single infrastructure recovery allowance.

The replacement aggregate ledger retains all three original reservations: 10.36 GPU-hours, with their full operation bounds. This conservatively covers uncommitted calls, including calls that could not seal a final receipt. Recovery proofs are retained in the private run-specific evidence directory, alongside prior physical cost and launch admission. No dataset, optimizer, seed, LR, algorithm or ordering was changed.
