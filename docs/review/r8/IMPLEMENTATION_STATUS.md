# R8 implementation status (2026-09-25 Asia/Shanghai)

Status: **IN PROGRESS; NO R8 MODEL OR TARGET JOB STARTED.**

The user authorized the R8 plan on `jiangsuiyang` physical GPUs 5/6/7 and requested hourly monitoring with bounded repair and continuation. The older statement in the attached design that the then-current request was only to write a plan is superseded by this newer user request. The numerical, identity, and resource boundaries in the plan remain the implementation target.

This worktree is `experiment/r8-ba-performance-envelope-v1`, based on R7 handoff commit `5c1d4588094d7967e5f1c81a7d7d6896470948ca`. `docs/review/r8/input/` contains the six original ZIP members. `scripts/r8/manifest.py` expands a disabled static graph with 65 source training and 724 target jobs, totaling 2,325,592 target arrivals; selected A/B IDs remain placeholders until source-only selection. The stress jobs retain source-seed identities for seed-bound R8 arms. `src/dpa_ctta/r8_ba/streams.py` verifies the five existing registered order sequences and fixes one local-seed MIXED permutation plus order0 repeated ten times for LONG10; production image/checkpoint binding has not yet been executed. `src/dpa_ctta/r8_ba/` also has parameterized FiLM, A/B/MLP modules, source schedules, oracle/basis primitives, a 32-visit/8-visit source trainer, independent 1024-step calibration primitive, source-val selection functions, shared zero-FiLM scaler observations, an image-only A/B/MLP target host, and separate B_G1/G3/COLD_G3 gradient diagnostic host. The latter binds source-fit RMS scale, LR, and pinned GraTa transform commit, and counts 8F/10F with real backward/Adam steps on synthetic input. R8 policy and method digests bind amplitude, rank, observer and auxiliary weight. Source training persists 250-step complete snapshots; the target journal persists 50-visit host/output snapshots in alternating verified slots. Both keep physical work append-only and permit one recovery, with target logical output rollback. CPU synthetic checks include source and target state roundtrips, durable replay checks, and weights-only serialization; they do not establish full real-data or GPU execution.

Missing before a real launch: integration of source oracles/trainer/five independent snapshot calibrations/selection and CURRENT-MLP into a bounded runner; source-cal LR selection and production wiring of B gradient arms; all native baselines; complete native method state snapshot/resume; executable source/target file binding; full cost ledgers and hard-cap enforcement; maximum-rank and native-method GPU profiles; measured capacity projection; clean code SHA, launch receipt, and end-to-end checks. `resources.py` has the full-graph 1.3-margin projection and aggregate cap guard, with a disabled 21-unit profile template; no unit has yet been measured on real GPU, so this gate cannot pass. `online.py` connects the image-only target reader, R8 host, packed prediction journal, and caller-supplied resource guard; failed model calls with observed partial counters remain in the append-only physical log. The online journal now binds the complete ordered target metadata to its snapshots and digest-verified completion receipt. `scoring.py` opens labels only after verifying that receipt and emits a separate private scalar file with its own completion receipt. Production orchestration and actual cap-ledger integration are still required. The user fixed the two ZIP-underdetermined choices on 2026-09-25: direct latent capacity Adam LR 0.03 and soft Dice for source-val selection. They are bound in `src/dpa_ctta/r8_ba/protocol.json` and the static task-graph digest. `capacity.py` implements one source-val-only 128-step probe on the original support/query identities, but no real probe has run. R7 jobs and artifacts must remain untouched. No external review PASS or resource qualification has been claimed.

Remote check on 2026-09-25: `jiangsuiyang` is `zmic44`; GPUs 5/6/7 are RTX 3090 24 GiB and showed about 24,124 MiB free each. `/data_nas/jiangsuiyang` is an NFS mount with 27 TiB available. These are point-in-time observations, not launch preflight. The hourly Codex heartbeat is `r8-b-a`; it should stay quiet on unchanged state and pause after final completion.

Metadata-only input audit on 2026-09-25: the preserved private R7 source manifest/split and target registration pass the existing group-isolation and all-five-order validators (159 source groups, fit/cal/val 111/23/25; five orders of 1,951 arrivals and 1,695 scored contents). A separate private R8 binding record holds their digests and the frozen MIXED/LONG10 sequence digests. The registered original checkpoint's 90,388,078-byte remote file still matched its recorded SHA256. This does not qualify the new model runner or constitute formal launch; zero source/target RGB or masks were decoded in this audit.

The source journal now retains separate digest-verified archives at steps 1,000/4,000/8,000/12,000/16,000, so later recovery-slot rotation cannot erase the five snapshots needed for independent calibration. The 20 synthetic R8 tests passed after this change; no real GPU profile or job has run.

The CURRENT-MLP source fit path now uses the shared visit schedule with its defined query segmentation loss and no clean-observation/proxy call. A synthetic optimizer step verifies the MLP head updates. All 21 R8 synthetic tests pass; the complete runner and real resource profile remain pending.

The selected-source loader now rebuilds A/B or CURRENT-MLP from a saved source point while checking the fit step, job binding, seed, method configuration, full weight digest, fitted observer, and basis. It leaves the active RNG and physical counters untouched so each independent calibration can start cleanly. All 22 R8 synthetic tests pass.

The source fit runner now guards every backbone forward, persists each physical step, records partial failed calls, and seals completion only after all 16,000 steps and five selected archives validate. Source and target recovery calls require an explicit evidenced infrastructure classification and remain limited to one per job. All 24 synthetic R8 tests pass. This is the fit component, not the complete 65-job source scheduler or formal launch.

`inputs.py` now reuses the registered source/target metadata audit and verified source reader, rejects any GPU other than physical 5/6/7 before decoding, and constructs an R8 segmenter from the exact registered checkpoint under a bounded context. It rebinds the metadata immediately before opening source inputs and rechecks used files on exit. A local metadata-only read of the preserved private registries found 159 groups, fit/cal/val 111/23/25 and zero registered overlap; a deliberately altered in-memory binding was rejected before source access. No real source pixels, checkpoint model, or GPU profile has been run through this new loader. All 25 R8 synthetic tests pass.

The source calibration runner now uses 250-step verified state snapshots for each of the five independent 1,024-step calibrations. Recovery uses the same one-use marker as its parent source fit job, preserves append-only physical work, and refuses non-infrastructure classifications. Full calibration seals a method artifact only after verifying step coverage and selected fit-point identity. A synthetic interruption/replay/completion check passed; all 26 R8 synthetic tests pass. This does not yet connect the 65 source jobs to val selection or qualify real GPU resources.

The user-confirmed capacity LR and source-val soft Dice metric are now pinned to a protocol SHA in the disabled full task graph. A source-val capacity primitive compares zero intervention, ambient oracle, oracle projection, and 128-step directly optimized latent with the same support/query identities. Its synthetic probe checks 128 physical backward/Adam calls and four query diagnostics; all 27 R8 synthetic tests pass. No real capacity result or source-val selection score exists yet.

The R8 deployment context now requires the exact frozen protocol SHA as part of its source identity. A mismatched protocol digest is rejected before an online host is constructed; the 27 synthetic tests still pass. Formal runner binding and real resource qualification remain pending.

On 2026-09-25 a bounded preflight on physical GPU 5 used the registered checkpoint and all 159 verified source groups under R8 code SHA `beb6514d037d87c4e4f37628e76e9a00efd9e967` and pinned upstream commit `dbff0d985c6c95345d9fb78f5b1daef57b392564`. One rank64 B fit optimizer step with a synthetic full-rank basis and zero proxy oracles completed in 1.608 seconds, counted 16 backbone forwards, and peaked at 2,317,750,784 allocated GPU bytes. The full source decode/read/check and preflight took 24.837 seconds; see `PROFILE_CRITICAL.surrogate.json`. This is resource evidence only, not a valid source preparation artifact, real training job, complete 21-unit profile, or full-matrix cap projection. No R8 fit or target job has started.

Additional bounded preflight on physical GPU 5 at code SHA `d4ae04658e23776606677b0eb515049fa83dd635` measured eight source-unit surrogates with real source RGB/labels, including oracle, capacity, basis VJP, rank64 fit, calibration, and validation; see `PROFILE_SOURCE_UNITS.surrogate.json`. Physical GPU 6 at code SHA `b683c2aba878d336106a9f0510ca3b79ed40c374` measured ten target-method surrogates using registered source RGB (no target image or label), including B gradient G1/G3 and native VPTTA/C/G; see `PROFILE_TARGET_UNITS.surrogate.json`. The latter used an isolated R8 Python environment for pinned `batchgenerators==0.25.2`, leaving the shared py38 environment unchanged. These are short-sample computation measurements, not an end-to-end target I/O profile or same-code 21-unit full-matrix qualification. An indicative estimate using three *unmeasured assumptions* for gradient LR, R7 C, and scoring would be about 933 GPU-hours after the 1.3 margin; this number is not a launch gate result. No R8 source fit or target trajectory has started.

Physical GPU 7 then completed a 50-visit online I/O surrogate at code SHA `db934b60d80a4273aa4d86afaeb41a61b283a1a3`: registered target RGB only, synthetic untrained B64 weights, 100 real backbone forwards, 3,276,800 committed prediction bytes, 7,791 logical trace bytes, and a 50-visit journal snapshot in 8.697 seconds. No target label was opened. The first packaging attempt stopped before any model forward because the remote source-only archive lacked frozen `configs/r3_science_v1.json`; its failure record is preserved as `PROFILE_TARGET_IO.failed_packaging.json`. A same-commit archive of tracked `configs/` corrected the packaging and a new profile root completed; see `PROFILE_TARGET_IO.surrogate.json`. This is not a scientific R8 target job or full resource gate.

The source-only grid selector now requires all 48 discovery jobs, both seeds and five calibrated source points per mode, selects each config/mode checkpoint independently, and chooses A/B family configurations from FULL scores only with recorded source-plus-online costs for ties. A synthetic test covers the separate STATIC checkpoint and incomplete-grid rejection; 28 R8 synthetic tests pass. Actual validation rows and cost records do not yet exist.
