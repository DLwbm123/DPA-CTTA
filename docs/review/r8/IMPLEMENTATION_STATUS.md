# R8 implementation status (2026-09-25 Asia/Shanghai)

Status: **IN PROGRESS; NO R8 MODEL OR TARGET JOB STARTED.**

The user authorized the R8 plan on `jiangsuiyang` physical GPUs 5/6/7 and requested hourly monitoring with bounded repair and continuation. The older statement in the attached design that the then-current request was only to write a plan is superseded by this newer user request. The numerical, identity, and resource boundaries in the plan remain the implementation target.

This worktree is `experiment/r8-ba-performance-envelope-v1`, based on R7 handoff commit `5c1d4588094d7967e5f1c81a7d7d6896470948ca`. `docs/review/r8/input/` contains the six original ZIP members. `scripts/r8/manifest.py` expands a disabled static graph with 65 source training and 724 target jobs, totaling 2,325,592 target arrivals; selected A/B IDs remain placeholders until source-only selection. The stress jobs retain source-seed identities for seed-bound R8 arms. `src/dpa_ctta/r8_ba/streams.py` verifies the five existing registered order sequences and fixes one local-seed MIXED permutation plus order0 repeated ten times for LONG10; real registration binding has not yet been executed. `src/dpa_ctta/r8_ba/` also has parameterized FiLM, A/B/MLP modules, source schedules, oracle/basis primitives, a 32-visit/8-visit source trainer, independent 1024-step calibration primitive, source-val selection functions, shared zero-FiLM scaler observations, an image-only A/B/MLP target host, and separate B_G1/G3/COLD_G3 gradient diagnostic host. The latter binds source-fit RMS scale, LR, and pinned GraTa transform commit, and counts 8F/10F with real backward/Adam steps on synthetic input. R8 policy and method digests bind amplitude, rank, observer and auxiliary weight. Source training persists 250-step complete snapshots; the target journal persists 50-visit host/output snapshots in alternating verified slots. Both keep physical work append-only and permit one recovery, with target logical output rollback. CPU synthetic checks include source and target state roundtrips, durable replay checks, and weights-only serialization; they do not establish full real-data or GPU execution.

Missing before a real launch: integration of source oracles/trainer/five independent snapshot calibrations/selection and CURRENT-MLP into a bounded runner; source-cal LR selection and production wiring of B gradient arms; all native baselines; complete native method state snapshot/resume; executable source/target file binding; full cost ledgers and hard-cap enforcement; maximum-rank and native-method GPU profiles; measured capacity projection; clean code SHA, launch receipt, and end-to-end checks. `resources.py` has the full-graph 1.3-margin projection and aggregate cap guard, with a disabled 21-unit profile template; no unit has yet been measured on real GPU, so this gate cannot pass. `online.py` connects the image-only target reader, R8 host, packed prediction journal, and caller-supplied resource guard; failed model calls with observed partial counters remain in the append-only physical log. The online journal now binds the complete ordered target metadata to its snapshots and digest-verified completion receipt. `scoring.py` opens labels only after verifying that receipt and emits a separate private scalar file with its own completion receipt. Production orchestration and actual cap-ledger integration are still required. The ZIP leaves the latent capacity probe optimizer LR and the source-val selection Dice threshold convention unspecified; the user was asked to fix these choices. R7 jobs and artifacts must remain untouched. No external review PASS or resource qualification has been claimed.

Remote check on 2026-09-25: `jiangsuiyang` is `zmic44`; GPUs 5/6/7 are RTX 3090 24 GiB and showed about 24,124 MiB free each. `/data_nas/jiangsuiyang` is an NFS mount with 27 TiB available. These are point-in-time observations, not launch preflight. The hourly Codex heartbeat is `r8-b-a`; it should stay quiet on unchanged state and pause after final completion.

Metadata-only input audit on 2026-09-25: the preserved private R7 source manifest/split and target registration pass the existing group-isolation and all-five-order validators (159 source groups, fit/cal/val 111/23/25; five orders of 1,951 arrivals and 1,695 scored contents). A separate private R8 binding record holds their digests and the frozen MIXED/LONG10 sequence digests. The registered original checkpoint's 90,388,078-byte remote file still matched its recorded SHA256. This does not qualify the new model runner or constitute formal launch; zero source/target RGB or masks were decoded in this audit.
