# B3 frozen balanced order execution contract

Authorized by the user's B3 plan on 2026-09-11. Baseline publication `3a6418b9f83485aa2f304484c17bfa42dfb6e231`. Scientific changes: none. Preserve prior B2 conclusion and all existing source/config/report files. Only full domain-block ordering changes; within-block order, identities, subsets and source checkpoint are inherited.

Order2: ORIGA, Drishti_GS, REFUGE, REFUGE_Valid. Order3: REFUGE_Valid, REFUGE, Drishti_GS, ORIGA. Combined with original/reverse orders, each domain occupies each block position once and all twelve directed adjacent pairs appear once. This is four of 24 permutations, not equal cumulative image counts or complete-history balance. Same content/seed across orders; a post-B2 development decision, not independent patient replicates or previously blind validation.

Frozen direct entrypoints: native `make_host('fundus','A',state)`, B1 `Host('C')`, B2 `Host('U'/'S'/'I')`. The new Entry only normalizes return metadata, observes counts and retains native RNG. A keeps original source .grad, memory and AdaBN counters; lr .05, betas .9/.99. C/U/S/I keep 41 standard BN layers / 19,136 affine scalars, lr 1e-4 and betas .9/.999. S's local seed still begins `B2:20260907`; no change to weak/strong augmentation or its RNG. Cross-domain optimizer, memory and visit histories never reset. Final original-image prediction precedes mask access.

Five relevant CPU checks: combinatorial balance/coverage/budget; N identity reordering and scalar error rejection; actual-model direct/entry parity for all five arms; neutral Git subprocess transport; full CPU closeout on scalar fixtures. GPU smoke uses two fixed procedural images per direct/entry path and each arm: exactly 20 Adam/backwards and 136 segmentation forwards. Strict determinism is scoped to smoke, formal execution is a fresh process with the original backend configuration. This does not re-test complete native memory lifetime; prior lifecycle evidence remains historical.

Ten trajectories run sequentially: order2 A/C/U/S/I, then order3 A/C/U/S/I. Exactly 19,510 new scored updates/backwards, 132,668 segmentation forwards; including smoke 19,530 updates/backwards and 132,804 forwards. A's first 16 visits have two Prompt calls each, later visits have three due to retrieval. Each pinned Prompt.forward contains one fft2 and one ifft2; these derived FFT call counts are reported separately from measured segmentation forwards. No new teacher/source/DD/selector/outer/inner training. One GPU 4–7 (prefer7), eight active hours, 1 GiB private output. No retries, extra arms, seeds or radius search.

Old order0/1 A comes from P2, C from B1 and U/S/I from B2; each complete old trajectory is CPU validated. Stateless N is rearranged by group identity; no new N inference. G/O2/D4 are shown only as old-order context. Every new row includes native or BN-appropriate parameter/state/counter checks, metrics and existing scalar diagnostics. C/A interval fields are null. No maps, images, masks, model or optimizer snapshots are persisted; private rows/receipts/streams remain on NAS (0700/0600).

All new runtime argv are neutral Python entry strings with paths/stages/receipts passed via environment. A scoped runtime adapter moves Git working directories out of argv. For legacy path-scoped archive verification it reads the same selected committed blobs via ls-tree and cat-file --batch, reconstructing the in-memory tar consumed by the unchanged validator. Paths and object requests remain in-process/stdin; neutral Git argv and absence of child processes are tested via trace. Process ps and GPU process-name audits run at smoke/formal initialization. The detached parent also uses the neutral entry and launches both subsequent stages the same way. No pre-existing process is changed or stopped.

Private registration binds inherited file metadata, four private identity streams and old receipts; no new selection or asset hashing. Execution commit/config/registration/GPU/smoke are frozen in receipts. Failure records preserve actual completed prefix/cost and stop. The launcher executes formal run then independent CPU reconstruction exactly once, with no periodic monitor. At completion publish only source/config/tests, deidentified aggregate/cost/verification and report, separately from the frozen execution commit.

Report new orders separately from four-order combined means. Preserve all subsets, per-domain/channel metrics, paired tails/signs, conditional and joint-valid ASSD, I-C/I-U/I-S and all four versus A. Report each domain at each block position, method and paired order min/max/range. ASSD pixel maps are discarded and cannot be reconstructed. Keep Drishti_GS at one-quarter domain weight despite its 37 remaining_dev groups. Accuracy, worst-order behavior and channel tradeoffs are separate endpoints. No independent/significance/clinical/conformal claims, GT-driven selection or automatic next experiment.
