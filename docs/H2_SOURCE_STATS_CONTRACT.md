# H2 source-statistics contract — registration blocked

Status: **BLOCKED_TARGET_REGISTRATION**. The new JSON configuration is a disabled protocol draft, not a runnable or asset-frozen configuration. No H implementation, H2 entry, new behavioral tests or experiment receipt exists at this checkpoint. The user-requested registration hard stop precedes those stages. Old guards and the source-only whitelist remain unchanged.

The supplied H2 task fixes one H, N/A/B/H, one seed and domain order, and physical GPU 7. H replaces only the normalization output statistics of every original AdaBN with checkpoint source statistics; native warm-up loss construction, count, estimator, detach, epsilon and task-specific layer aggregation remain. H uses its own activations. Standard BN and original A/B behavior remain unchanged. A/H identity logits and isolated-layer loss equivalence are different checks; whole-model A/H loss equality is not required.

The draft JSON records the prescribed models, domains, selection/playback, hyperparameters, label isolation, state lifetime, tolerance, compute budget, metrics, research thresholds and failures. It does not register absent eligible assets. Before any future run, establish and freeze development/reserved roles, recorded prior use and known association constraints; then select existing content groups deterministically. Original CSV files and splits must remain unchanged. Unknown linkage requires a documented protocol treatment, not an invented claim of independence. Previously evaluated material cannot be designated untouched final evaluation.

All target registration precedes CPU regression and GPU use. The planned budget is 106 smoke + 52 source + 672 target = 830 Adam calls; 104 source and 896 target scoring records. Extra forwards are separately counted. Receipt must bind clean execution/config/dependency/source/target/GPU identities; formal execution additionally binds the exact smoke digest and coverage. Source and target hosts are separate fresh runs; task arms are independent; no domain-boundary resets.

When admissible, tests must cover identity/FFT, effective prompt gradients, isolated native loss preservation, complete layer and loss coverage, unchanged A/B, lifecycle and source immutability, label isolation, negative registrations/records, failure prefix and smoke binding. None were run for H2 at this blocked checkpoint. Historical H1 test counts are not H2 passes.

No image/mask/logit/feature/model/optimizer output is authorized. Runtime scalars and private registration require owned 0700/0600 paths and a 256 MiB cap. Source/proxy assets retain original identities and selection. Scientific underperformance does not stop a valid registered flow; engineering failure does, without continuation or automatic retry. Independent CPU reconstruction must validate every completed record and actual count before a completed-run claim.

This delivery ends the blocked task. It starts no background worker, scheduled monitor or automatic recovery. Resolving the missing registration requires explicit role/association evidence or a user-approved revised protocol; it does not justify silent reassignment of dataset splits.
