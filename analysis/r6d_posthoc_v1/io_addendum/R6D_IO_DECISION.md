# R6-D 预检阻塞：输入适配修复建议

## 结论

保留既有尝试 R6D_INCOMPLETE。下一步建议局部修复输入布局适配，再开展一次新的前台只读诊断；不改变科学 gate，不转入 R6-B，不新增模型计算。

这份材料是源码核对及续办规格，不是新分析结果、工程PASS或执行receipt。

## 已核对的依据

[S1] 失败报告记载：原顶层 public_aggregate.json 是指向 current/public_aggregate.json 的符号链接；普通文件预检拒绝，快照为0，未开始逐行校验或机制分析。25/25为提交方工具测试成绩，本次没有重跑。

[S2] 原发布器 r1/evidence.py::publish 确实生成顶层聚合别名、current版本目录别名，以及带 result_directory 的权威 current_result.json。因此该链接布局不是本身的损坏证据。

[S3] 旧分析 run.py::allowed_sources 把顶层别名列入普通数据输入；main 对它调用 regular 后，在创建snapshot与主要异常/遥测范围之前退出。核心 regular 明确拒绝symlink及nlink!=1。这与已报告失败吻合。

[S4] 原公开执行审计将选定版本记为 results/7b2e24f345f0463fa8ee451888650e66。本次不能替代服务器端的现有指针/链接核对；续跑必须同时验证版本与历史绑定。

[S5] 读取的README、只读审计和报告明确区分工具测试与真实分析；本次未发现应把该预检失败解释成科学负结果的依据。

## 最小修订

原计划“禁止源符号链接”的表述没有区分发布别名与实际负载。补充规范现在允许对两个已知别名做元数据审计，但仍不经别名读取负载。实际复制固定版本的普通文件，快照仍为独立普通文件。

不删除regular，不修改源链接，不递归解引用，不调用历史重算/发布函数。逻辑快照名public_aggregate.json与实际版本文件路径分别记录。

链接lstat/readlink元数据、权威指针原始字节、白名单负载及快照摘要分别核对；源读取与快照只允许原始字节复制。Python官方文档区分lstat（不跟随链接）、readlink（读取链接目标文本）、fstat（实际已打开对象）以及is_file（也可对指向普通文件的链接返回true）。这些接口不是完整OS沙箱，不能扩大其保证。

预检纳入首失败记录和耗时/RSS边界；finally中的复核异常不得覆盖首错。此项属于本次输入链的异常记账，不扩展研究范围。

## 不变的科学约束

原R6D分析规格 SHA256为 ffd3c648931e27130c2c275da7ca83f7bac62906b9e9e3d51ec4b3fd62d07368；本次从现有原交付ZIP中重新计算一致。旧字节保持，新I/O补充有独立摘要。

12条轨迹、各1951内容、两类日志各23412行；所有配对、分层、窗口、恒等式、指标校验与原gate不变。原R6-A保持NO_ADVANCE，R6-B未运行；模型forward/backward/Adam/VJP预算仍为0。

## 执行顺序

局部适配修复 → 已有25项加针对性标量/I/O测试 → 固定新分析SHA → 新目录单次前台诊断 → 前后只读核对与完整报告。

无需重跑166项模型回归，不要求为只读标量续办再建GPU式审阅循环。此Prompt被用户作为新任务交给执行方时，才构成该次显式续办指令；不声称它已经发生。

## 本次核对边界

读取了GitHub公开报告、run.py、core.py相关代码、README、原发布器及原执行审计；还核对了已提供原设计ZIP中的analysis spec摘要。

未读取用户本机 /Users/... 的交付ZIP，未访问服务器/私有账本，未修改远端代码，未运行修复测试或真实诊断。没有生成新的机制分析表。
